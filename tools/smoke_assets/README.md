Smoke assets for `tools/llm_smoke.cpp`.

- `qwen3vl_smoke_image.png`: default image used by `llm_smoke` when running a VLM model config and no `--image/--video` is provided.
- To use a different image for a specific model directory, place `smoke_image.png` under that `model_dir/` (it takes priority).

